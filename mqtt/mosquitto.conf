# Local dev broker for mqtt/ scripts (default localhost:1883).
# From repo root: docker compose -f mqtt/docker-compose.yml up -d

persistence false
log_dest stdout

listener 1883 0.0.0.0
allow_anonymous true
